package com.zenself.mobile

import android.app.Application

class ZenSelfApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Future: initialize DataStore, WorkManager scheduling
    }
}
